{
  "name": "logs",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "http-server -c-1",
    "deploy":"firebase deploy --only hosting"
  },
  "keywords": [
    "logs"
  ],
  "devDependencies": {
    "http-server": "^0.12.3"
  }
}
